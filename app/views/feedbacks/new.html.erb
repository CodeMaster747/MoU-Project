<div class="d-flex align-items-center justify-content-between mb-3">
  <div>
    <h1 class="h3 mb-1">New Feedback</h1>
    <div class="text-muted"><%= @outcome.title %></div>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", mou: @mou, outcome: @outcome, feedback: @feedback %>
  </div>
</div>
